# TRENCHNET — LESSONS PORTED FROM SIBLING DESKS

Read-only study of sibling repos (no files modified there, no `.env`/pem/key files opened, no live modes started).
Every pattern below is adapted to a PAPER-ONLY, WATCH-ONLY Solana pump.fun observer.

| # | Lesson | Source (path + line/section) | How TRENCHNET applies it |
|---|--------|-------------------------------|--------------------------|
| 1 | Hygiene flags fire a **hard SKIP that beats force_bet** — veto order: hygiene first, then chop-regime edge floor, only then threshold logic. | `HermesTools/btc-15m-typesafe/bot/decision.py` — `HYGIENE_BOOL_FLAGS` (~L56), `hygiene_flags_fire()` (~L70), `decide()` order "hygiene → chop_edge_below_min → force_bet → side/threshold" (~L105) | TRENCHNET paper fills run hygiene gates BEFORE any fill: no real price sig → refuse; oversized vs typical buy → refuse; missing evidence → refuse; kill switch on → refuse. SKIP is a valid answer. |
| 2 | Shock alone vetoes; shock + follow-through does not — nuanced flags, not blanket bans. | `btc-15m-typesafe/bot/decision.py` — `hygiene_flags_fire()`: `shock_range and not follow_through` | Oversized-buy caution is one gate, not a permanent ban: refuse-with-reason states exactly which gate fired so the operator can re-run after reading the evidence. |
| 3 | Confidence/edge floors, config-driven: clear_edge 0.65, chop 0.75 — thresholds live in config, not code. | `btc-15m-typesafe/config/default.yaml` — `clear_edge_threshold: 0.65`, `chop_min_edge: 0.75`; `BTCC-FILTER-REVIEW.md` "Rules locked" table | Paper risk controls (act threshold, oversized-buy ratio, skip-streak halt, daily loss cap) live in `config/settings.yaml` `paper:` block and are editable from the Settings page — labeled PAPER risk controls. |
| 4 | Skip-streak halt: N consecutive non-dry-run SKIPs halts the loop; dry-run rows neither increment nor reset the streak. | `btc-15m-typesafe/bot/runner.py` — `consecutive_skip_streak()` (L201), `note_skip_streak()` (L216) + `config/default.yaml` `skip_streak_halt: 20`; rule confirmed in `data/btcc-receipts/BTCC_SIGNAL_HYGIENE.md` "Rules locked" #4 | TRENCHNET paper loop halts after `skip_streak_halt` consecutive refused fills; refusals are logged to the ledger with reasons so the streak is auditable. |
| 5 | Ledger is append-only JSONL with `mode: PAPER` stamped on every row; atomic rewrite for edits. | `btc-15m-typesafe/bot/ledger.py` — `append_record()` (`row.setdefault("mode", "PAPER")`), `rewrite_all()` tmp+replace | `data/paper/ledger.jsonl` appends every paper buy/sell/close with `mode: "PAPER"`, `price_source_signature` (real on-chain tx), and refusal rows carry reasons. |
| 6 | LIVE aborts are hard runtime checks: env flag OR config `live:true` → raise; private-key env vars present → abort. | `alpha-engine/btcc_desk/nodes/paper_exec.py` (LIVE_TRADING + PRIVATE_KEY/WALLET_KEY/API_SECRET abort, ~L25-33); `alpha-engine/btcc_desk/qlib_lab/gates.py` — `assert_paper_only()` (whole file); `btc-15m-typesafe/bot/guards.py` — `LiveTradingAbort` | TRENCHNET asserts paper-only at fill time and server start: LIVE toggle is a **disabled UI stub with no implementation** ("LIVE is off until Jeremy names a ticket"); if private-key envs appear, fills refuse with `live_guard` reason. |
| 7 | Paper exec writes jsonl only — "NEVER venue HTTP — jsonl only"; fills carry explicit evidence fields. | `alpha-engine/btcc_desk/nodes/paper_exec.py` — fill dict `schema: btcc.paper_fill.v0` + comment "NEVER venue HTTP — jsonl only" (L43); `PAPER-LOOP-ACCEPTANCE.md` acceptance #4/#8/#10 | Paper fills are pure ledger writes priced from stored real trade events; zero swap/Jupiter/pump.fun order endpoints exist anywhere in the codebase. |
| 8 | Entry gates skip on price/probability: max entry ceiling, min side probability, with `no_quote` handling. | `HermesTools/kalshi-15m-live/entry_gate.py` — `should_skip_pricey()` (DEFAULT_MAX_ENTRY 0.80), `should_skip_weak_prob()` (DEFAULT_MIN_SIDE_PROB 0.70), `no_quote` reason | Analogous paper gates: no recent real trade price for the token → `no_price` refusal (never mark-to-invented); oversized buy vs `typical_buy_size_sol` → refusal with ratio in the reason. (Kalshi keys/pem were NOT opened.) |
| 9 | Kill switch = flag-file pattern; arm/kill is one obvious action; conf floor documented next to it. | `D:\Freebuff\15-min-BTC-JAP\README.md` — "Arm / kill `~/.beat15m/LIVE_MARK` = `live` / delete file" table row + "Kill switch: `rm ~/.beat15m/LIVE_MARK && pkill`" | `data/paper/KILL_SWITCH` flag file instantly blocks new paper fills (checked before every fill); Settings page has a Kill Switch button; flag state shown in the health panel. |
| 10 | Desk UI: big dark status tiles + SSE `/events` self-refresh; live trade feed. | `HermesTools/jev-dashboard/jev_dashboard.py` — status tile markup + `@app.route("/events")` SSE generator | TRENCHNET desk uses dark status tiles (events, wallets, routes, paper PnL) + `/api/events` SSE for live activity feed and auto-refresh. |
| 11 | Memecoin desk pattern: read-only PAPER badge in the header, self-refreshing Flask desk, free read-only enrichment, honest mock/fallback labeling. | `HermesVault/60-Trading/memecoin-desk/app.py` — docstring "Read-only PAPER mode" + fallback prints `WARNING ... using mock data` | Persistent WATCH-ONLY + PAPER badges on every page; any FCC template/Jev dry-run/empty state is labeled; nothing renders without real `out/` data ("no data yet"). |
| 12 | Desktop packaging: PyInstaller one-file desks (MEI magic confirmed) launched from shortcuts with custom icons. | `memecoin-desk` README/docstring — "Coin Desk.exe is PyInstaller (MEI magic confirmed)"; desktop `Kalshi 15m Desk.exe` same pattern | `build_desk.ps1` (PyInstaller) builds `dist\TRENCHNET Desk.exe`; both TRENCHNET.lnk shortcuts point at it (run_ui.bat fallback) with `trenchnet_radar.ico,0`. |

Honesty labels preserved in all outputs: `template report`, `Jev dry-run mock`, `PAPER`, `LIVE off (no implementation)`.


## Pass 5 backtest pitfalls (and guards)

- **Lookahead**: Walk-forward cuts are strictly time-ordered — train uses buys with lock_time < cut, test uses >= cut. Ranking never peeks at test trades. If priced overlap across cuts is too thin, we report insufficient_data instead of a fake correlation.
- **Survivorship**: Roster includes expanded co-buyers and thin wallets, not only historical winners. Scores still depend on who we tracked; the dashboard labels this observational.
- **Slippage / fees**: Copy sims apply pump.fun fee bps, priority fee, and slippage that scales up when the liquidity proxy (median nearby trade SOL) is thin. Browser sliders only rescale those baked costs — they do not invent fills.
- **Overfitting**: Composite weights live in config/backtest.yaml and are documented. Out-of-sample stability is a first-class feature; when OOS cannot be measured honestly, wallets are flagged insufficient_oos rather than marked stable.
- **Missing prices**: No fabricated candles. Unpriceable copies are counted and excluded from PnL. Birdeye USD and derived SOL/token series are never mixed on the same mint.
